# 🔧 Возможности AntiZapret

Полный список функций и возможностей системы AntiZapret для OPNsense/FreeBSD.

---

## 📋 Содержание

- [Основные возможности](#-основные-возможности)
- [Установщик](#-установщик)
- [Сетевые функции](#-сетевые-функции)
- [Tor и транспорты](#-tor-и-транспорты)
- [Интеграция с OPNsense](#-интеграция-с-opnsense)
- [Безопасность](#-безопасность)
- [Автоматизация](#-автоматизация)
- [Совместимость](#-совместимость)

---

## ✨ Основные возможности

### Прозрачный обход блокировок

| Функция | Описание |
|---------|----------|
| 🔄 **Прозрачный прокси** | Трафик перенаправляется автоматически без настройки клиентов |
| 🔒 **Поддержка HTTPS** | Полная поддержка шифрованного трафика |
| 📋 **Автообновление списков** | Ежедневное обновление списка заблокированных IP |
| 🌐 **Все устройства** | Работает для всех устройств в локальной сети |

### Что работает автоматически

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Без настройки на клиентах                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ✓ Компьютеры (Windows, macOS, Linux)                                  │
│  ✓ Смартфоны и планшеты (iOS, Android)                                 │
│  ✓ Smart TV (Samsung, LG, Sony и др.)                                  │
│  ✓ Игровые консоли (PlayStation, Xbox, Nintendo)                       │
│  ✓ Стриминговые устройства (Apple TV, Chromecast, Fire TV)             │
│  ✓ IoT устройства                                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Установщик

### Интерактивный установщик v3.0

| Функция | Описание |
|---------|----------|
| 🎨 **Красочный интерфейс** | 256 цветов, градиенты, ASCII-арт |
| 📊 **Прогресс-бары** | Визуальное отображение процесса установки |
| ⏳ **Анимированные спиннеры** | Индикация длительных операций |
| 🔍 **Автоопределение системы** | Версия ОС, архитектура, сетевые интерфейсы |

### Автоматическое определение

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Что определяет установщик                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Система:                                                               │
│  ├── Версия FreeBSD (13.x, 14.x)                                       │
│  ├── Архитектура (amd64, arm64)                                        │
│  ├── Наличие OPNsense                                                  │
│  └── Версия OPNsense                                                   │
│                                                                         │
│  Сеть:                                                                  │
│  ├── Все сетевые интерфейсы                                            │
│  ├── IP-адреса интерфейсов                                             │
│  ├── LAN IP для конфигурации                                           │
│  └── Default gateway                                                    │
│                                                                         │
│  Пакеты:                                                                │
│  ├── Актуальные версии в репозитории FreeBSD                           │
│  ├── Уже установленные пакеты                                          │
│  └── Необходимые зависимости                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Выбор пакетов

Установщик предлагает выбрать опциональные пакеты:

| Пакет | Описание |
|-------|----------|
| `mc` | Midnight Commander — файловый менеджер |
| `git` | Система контроля версий |
| `curl` | Утилита для передачи данных |
| `wget` | Загрузчик файлов |
| `nano` | Текстовый редактор |

### Установка из репозитория FreeBSD

Установщик автоматически:

1. Определяет версию FreeBSD и архитектуру
2. Формирует URL репозитория FreeBSD
3. Находит актуальные версии пакетов
4. Скачивает и устанавливает пакеты через `pkg add`

```bash
# Пример автоматически формируемых команд:
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/tor-0.4.8.12.pkg
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/obfs4proxy-tor-0.0.14_17.pkg
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/webtunnel-tor-0.0.1_10.pkg
```

---

## 🌐 Сетевые функции

### Порты и сервисы

| Порт | Сервис | Назначение |
|------|--------|------------|
| **9040** | TransPort | Прозрачный прокси для перенаправленного трафика |
| **9050** | SocksPort | SOCKS5 прокси для ручной настройки |
| **9053** | DNSPort | DNS-резолвер через Tor |

### SOCKS5 прокси

Помимо прозрачного прокси, доступен SOCKS5 для приложений с ручной настройкой:

```
Адрес: LAN_IP (например, 192.168.1.1)
Порт: 9050
Тип: SOCKS5
Аутентификация: нет
```

### DNS через Tor

DNS-запросы можно направлять через Tor для дополнительной приватности:

```
DNS-сервер: LAN_IP
Порт: 9053
```

### Виртуальные адреса

Для .onion доменов используется диапазон:

```
10.192.0.0/10
```

---

## 🧅 Tor и транспорты

### Поддерживаемые транспорты

| Транспорт | Пакет | Описание |
|-----------|-------|----------|
| **obfs4** | `obfs4proxy-tor` | Маскирует трафик под случайные данные |
| **webtunnel** | `webtunnel-tor` | Маскирует трафик под HTTPS |

### obfs4

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            obfs4 транспорт                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Tor Client → obfs4proxy → [случайные байты] → obfs4 bridge → Tor     │
│                                                                         │
│   Преимущества:                                                         │
│   ├── Высокая скорость                                                 │
│   ├── Много доступных мостов                                           │
│   ├── Низкие накладные расходы                                         │
│   └── Сложно детектировать                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### webtunnel

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          webtunnel транспорт                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Tor Client → webtunnel → [HTTPS трафик] → webtunnel bridge → Tor     │
│                                                                         │
│   Преимущества:                                                         │
│   ├── Выглядит как обычный HTTPS                                       │
│   ├── Использует порт 443                                              │
│   ├── Максимальная скрытность                                          │
│   └── Сложнее заблокировать                                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Сравнение транспортов

| Характеристика | obfs4 | webtunnel |
|----------------|-------|-----------|
| Скорость | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Скрытность | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Доступность мостов | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Устойчивость к DPI | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

### Интерактивная настройка мостов

Установщик поддерживает ввод bridge-строк для обоих типов транспорта:

```
▶ Enter OBFS4 bridge lines (empty line to finish)

  obfs4> Bridge obfs4 192.0.2.1:443 FINGERPRINT cert=CERT iat-mode=0
  obfs4> Bridge obfs4 192.0.2.2:9001 FINGERPRINT cert=CERT iat-mode=0
  obfs4>

▶ Enter WebTunnel bridge lines (empty line to finish)

  webtunnel> Bridge webtunnel 192.0.2.3:443 FINGERPRINT url=https://example.com/
  webtunnel>
```

### IPv6 поддержка

Настраиваемая поддержка IPv6:

```ini
ClientUseIPv6 1          # Использовать IPv6
ClientUseIPv4 1          # Использовать IPv4
ClientPreferIPv6ORPort 1 # Предпочитать IPv6
```

---

## 🖥️ Интеграция с OPNsense

### Автоматическое создание action-скриптов

Установщик создаёт скрипты для интеграции с OPNsense configd:

#### AntiZapret Action

```
/usr/local/opnsense/service/conf/actions.d/actions_antizapret.conf
```

| Команда | Описание |
|---------|----------|
| `cron-iplist-renew` | Обновление списка заблокированных IP |

#### Tor Service Actions

```
/usr/local/opnsense/service/conf/actions.d/actions_tor.conf
```

| Команда | Описание |
|---------|----------|
| `start` | Запуск Tor |
| `stop` | Остановка Tor |
| `restart` | Перезапуск Tor |
| `status` | Статус Tor |

### Firewall Alias

Поддержка External Alias для автоматического обновления списка IP:

```
Тип: External (advanced)
URL: https://LAN_IP/ipfw_antizapret.dat
```

### NAT Port Forward

Правило для перенаправления трафика на Tor:

| Параметр | Значение |
|----------|----------|
| Interface | LAN |
| Protocol | TCP |
| Destination | AntiZapret_IPs (alias) |
| Redirect target | 127.0.0.1:9040 |

### Cron задания

Автоматическое обновление списков через планировщик OPNsense:

| Время | Команда |
|-------|---------|
| 04:00 daily | Renew AntiZapret IP-list |

---

## 🛡️ Безопасность

### Исключение узлов

По умолчанию исключаются узлы из СНГ и соседних стран:

```ini
ExcludeNodes {RU},{BY},{KG},{KZ},{UZ},{TJ},{TM},{TR},{AZ},{AM},{GE},{MD},{UA}
ExcludeExitNodes {RU},{BY},{KG},{KZ},{UZ},{TJ},{TM},{TR},{AZ},{AM},{GE},{MD},{UA}
StrictNodes 1
```

### Режим только клиента

Система настроена как клиент Tor, не как relay или exit-нода:

```ini
ExitPolicy reject *:*
ExitPolicy reject6 *:*
ExitRelay 0
```

### Резервное копирование

Установщик автоматически создаёт резервные копии существующих конфигураций:

```
/usr/local/etc/tor/torrc.backup.YYYYMMDD_HHMMSS
```

### Верификация установки

После установки проводится автоматическая проверка:

| Проверка | Описание |
|----------|----------|
| Процесс Tor | Запущен ли tor |
| SOCKS порт | Слушает ли порт 9050 |
| DNS порт | Слушает ли порт 9053 |
| TransPort | Слушает ли порт 9040 |
| Конфигурация | Наличие torrc |
| Список IP | Наличие и размер файла |
| Автозапуск | Включён ли в rc.conf |

---

## ⚙️ Автоматизация

### Автозапуск Tor

Автоматический запуск при загрузке системы:

```bash
sysrc tor_enable="YES"
```

### Скрипт обновления списков

```bash
/root/antizapret/antizapret.pl
```

Скрипт:
- Скачивает актуальный список заблокированных ресурсов
- Извлекает IP-адреса
- Сохраняет в `/usr/local/www/ipfw_antizapret.dat`

### Ежедневное обновление

Через cron OPNsense:

```bash
# Обновление списка и добавление в таблицу pfctl
/root/antizapret/antizapret.pl | tee /usr/local/www/ipfw_antizapret.dat | xargs pfctl -t AntiZapret_IPs -T add
```

---

## 💻 Совместимость

### Поддерживаемые системы

| Система | Версия | Статус |
|---------|--------|--------|
| **OPNsense** | 24.x | ✅ Полная поддержка |
| **OPNsense** | 23.x | ✅ Поддерживается |
| **FreeBSD** | 14.x | ✅ Полная поддержка |
| **FreeBSD** | 13.x | ✅ Поддерживается |
| **pfSense** | 2.7+ | ⚠️ Не тестировалось |

### Поддерживаемые архитектуры

| Архитектура | Статус |
|-------------|--------|
| **amd64** | ✅ Основная |
| **arm64** | ✅ Поддерживается |
| **i386** | ⚠️ Ограниченная |

### Источники пакетов

| Пакет | OPNsense repo | FreeBSD repo |
|-------|---------------|--------------|
| `tor` | ✅ Есть | ✅ Есть |
| `obfs4proxy-tor` | ❌ Нет | ✅ Есть |
| `webtunnel-tor` | ❌ Нет | ✅ Есть |
| `zstd` | ✅ Есть | ✅ Есть |

---

## 📁 Структура файлов

### После установки

```
/root/antizapret/
├── install.sh                 # Установщик v3.0
├── antizapret.pl              # Скрипт обновления списков
├── README.md                  # Документация
├── QUICKSTART.md              # Быстрый старт
├── INSTALL.md                 # Полная инструкция
├── FEATURES.md                # Этот файл
├── FAQ.md                     # Часто задаваемые вопросы
├── TOR.md                     # Настройка Tor
├── WireGuard.md               # Альтернатива с VPN
├── CHANGELOG.md               # История изменений
└── DEMO.txt                   # Пример вывода установщика

/usr/local/etc/tor/
└── torrc                      # Конфигурация Tor

/usr/local/www/
└── ipfw_antizapret.dat        # Список заблокированных IP

/var/log/tor/
└── notices.log                # Логи Tor

/usr/local/opnsense/service/conf/actions.d/
├── actions_antizapret.conf    # Action для обновления списков
└── actions_tor.conf           # Actions для управления Tor

/usr/local/bin/
├── tor                        # Tor daemon
├── obfs4proxy                 # obfs4 транспорт
└── webtunnel-tor-client       # webtunnel транспорт
```

---

## 🔮 Планы развития

### Версия 3.1 (планируется)

- [ ] 🌐 Веб-интерфейс для управления
- [ ] 📊 Статистика использования
- [ ] 🔔 Уведомления о проблемах
- [ ] 🔄 Автоматическое обновление скрипта

### Версия 3.2 (планируется)

- [ ] 🐳 Docker-образ
- [ ] 📱 Мобильное приложение для мониторинга
- [ ] 🔧 Поддержка других роутеров

---

## 📚 Связанная документация

| Документ | Описание |
|----------|----------|
| [README.md](README.md) | Общее описание проекта |
| [QUICKSTART.md](QUICKSTART.md) | Установка за 2 минуты |
| [INSTALL.md](INSTALL.md) | Полное руководство |
| [TOR.md](TOR.md) | Детали настройки Tor |
| [FAQ.md](FAQ.md) | Ответы на вопросы |
| [CHANGELOG.md](CHANGELOG.md) | История изменений |

---

## 💖 Поддержать проект

Если AntiZapret оказался полезен:

<div align="center">

**USDT (TRC20)**

```
TCyZuUjX3ymFmrDPxTmeSNPMuuWRDtviFy
```

</div>

---

<div align="center">

Основано на [Limych/antizapret](https://github.com/Limych/antizapret)

**[⬆ Вернуться наверх](#-возможности-antizapret)**

</div>
