# 🧅 Настройка Tor для AntiZapret

Подробное руководство по настройке Tor с транспортными плагинами obfs4 и webtunnel для обхода блокировок.

---

## 📋 Содержание

- [Обзор](#-обзор)
- [Архитектура](#-архитектура)
- [Установка](#-установка)
- [Настройка мостов](#-настройка-мостов)
- [Конфигурация torrc](#-конфигурация-torrc)
- [Транспортные плагины](#-транспортные-плагины)
- [Мониторинг](#-мониторинг)
- [Решение проблем](#-решение-проблем)
- [Безопасность](#-безопасность)

---

## 🌐 Обзор

### Что такое Tor?

**Tor** (The Onion Router) — это сеть для анонимизации интернет-трафика. В контексте AntiZapret мы используем Tor как прозрачный прокси для обхода блокировок.

### Преимущества использования Tor

| Преимущество | Описание |
|--------------|----------|
| 🔒 **Анонимность** | Ваш реальный IP скрыт от целевых сайтов |
| 🌍 **Децентрализация** | Нет единой точки отказа |
| 🆓 **Бесплатность** | Не требует платных VPN-сервисов |
| 🔧 **Надёжность** | Работает даже при частичных блокировках |
| 🛡️ **Устойчивость** | Мосты позволяют обойти блокировку самого Tor |

### Компоненты системы

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         AntiZapret + Tor                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐    ┌───────────────┐    ┌──────────┐    ┌─────────────┐  │
│  │ Клиент   │───▶│  OPNsense     │───▶│   Tor    │───▶│  Интернет   │  │
│  │          │    │  NAT + Alias  │    │  Proxy   │    │             │  │
│  └──────────┘    └───────────────┘    └──────────┘    └─────────────┘  │
│                                                                         │
│  Компоненты Tor:                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  ● TransPort (9040) — прозрачный прокси для TCP                 │   │
│  │  ● SocksPort (9050) — SOCKS5 прокси                             │   │
│  │  ● DNSPort (9053)   — DNS резолвер через Tor                    │   │
│  │  ● obfs4proxy       — маскировка под случайный трафик           │   │
│  │  ● webtunnel        — маскировка под HTTPS                      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Архитектура

### Как работает прозрачный прокси

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Поток трафика                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   1. Клиент запрашивает blocked-site.com                                │
│      │                                                                  │
│      ▼                                                                  │
│   2. DNS-запрос → OPNsense                                             │
│      │                                                                  │
│      ▼                                                                  │
│   3. IP-адрес сайта определён: 1.2.3.4                                 │
│      │                                                                  │
│      ▼                                                                  │
│   4. OPNsense проверяет: 1.2.3.4 ∈ AntiZapret_IPs?                     │
│      │                                                                  │
│      ├── НЕТ → прямое подключение к сайту                              │
│      │                                                                  │
│      └── ДА → NAT redirect на 127.0.0.1:9040 (Tor TransPort)           │
│               │                                                         │
│               ▼                                                         │
│   5. Tor создаёт цепочку из 3 узлов                                    │
│      │                                                                  │
│      ▼                                                                  │
│   6. Трафик выходит через exit-ноду в другой стране                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Порты и их назначение

| Порт | Протокол | Назначение | Доступ |
|------|----------|------------|--------|
| **9040** | TCP | Transparent Proxy | Только localhost |
| **9050** | TCP | SOCKS5 Proxy | LAN + localhost |
| **9053** | UDP/TCP | DNS over Tor | LAN + localhost |

---

## 📦 Установка

### Автоматическая установка (рекомендуется)

Используйте основной установщик AntiZapret — он автоматически найдёт актуальные версии пакетов:

```bash
cd /root/antizapret
./install.sh
```

### Ручная установка пакетов

> ⚠️ **Важно:** Пакеты `obfs4proxy-tor` и `webtunnel-tor` **отсутствуют** в репозитории OPNsense. Их необходимо устанавливать вручную из репозитория FreeBSD по прямой ссылке.

#### Шаг 1: Определение параметров системы

```bash
# Версия FreeBSD (major)
OS_VERSION=$(uname -r | cut -d. -f1)

# Архитектура
ARCH=$(uname -m)

# Базовый URL репозитория FreeBSD
PKG_BASE="https://pkg.freebsd.org/FreeBSD:${OS_VERSION}:${ARCH}/latest/All"

echo "Система: FreeBSD ${OS_VERSION} (${ARCH})"
echo "Репозиторий: ${PKG_BASE}"
```

#### Шаг 2: Поиск актуальных версий пакетов

Версии пакетов регулярно обновляются. Найдите актуальные версии:

```bash
# Открыть в браузере или через fetch:
echo "Откройте: ${PKG_BASE}/"

# Или найти через командную строку:
fetch -qo - "${PKG_BASE}/" 2>/dev/null | grep -oE '(zstd|tor|obfs4proxy-tor|webtunnel-tor)-[0-9][^"]*\.pkg' | sort -u
```

#### Шаг 3: Установка пакетов

```bash
# Замените версии на актуальные!

# 1. Зависимость zstd
pkg add ${PKG_BASE}/zstd-1.5.6.pkg

# 2. Tor
pkg add ${PKG_BASE}/tor-0.4.8.12.pkg

# 3. obfs4proxy (обфускация трафика)
pkg add ${PKG_BASE}/obfs4proxy-tor-0.0.14_17.pkg

# 4. webtunnel (опционально, маскировка под HTTPS)
pkg add ${PKG_BASE}/webtunnel-tor-0.0.1_10.pkg
```

#### Шаг 4: Проверка установки

```bash
# Установленные пакеты
pkg info | grep -E "^tor-|obfs4|webtunnel|zstd"

# Бинарные файлы
which tor obfs4proxy webtunnel-tor-client
```

### Скрипт автоматического поиска и установки

Создайте скрипт напрямую на роутере:

```bash
cat > /root/install_tor_plugins.sh << 'ENDSCRIPT'
#!/bin/sh

echo "=== Определение системы ==="
OS_VERSION=$(uname -r | cut -d. -f1)
ARCH=$(uname -m)
PKG_BASE="https://pkg.freebsd.org/FreeBSD:${OS_VERSION}:${ARCH}/latest/All"

echo "FreeBSD ${OS_VERSION} (${ARCH})"
echo "Репозиторий: ${PKG_BASE}"
echo ""

# Функция поиска и установки пакета
install_pkg() {
    PKG_NAME=$1
    
    if pkg info "$PKG_NAME" >/dev/null 2>&1; then
        echo "✓ ${PKG_NAME} уже установлен"
        pkg info "$PKG_NAME" | head -1
        return 0
    fi
    
    echo "Поиск ${PKG_NAME}..."
    PKG_FILE=$(fetch -qo - "${PKG_BASE}/" 2>/dev/null | grep -oE "${PKG_NAME}-[0-9][^\"]*\.pkg" | sort -V | tail -1)
    
    if [ -n "$PKG_FILE" ]; then
        echo "Найден: ${PKG_FILE}"
        echo "Установка..."
        if pkg add "${PKG_BASE}/${PKG_FILE}"; then
            echo "✓ ${PKG_NAME} установлен"
        else
            echo "✗ Ошибка установки ${PKG_NAME}"
            return 1
        fi
    else
        echo "✗ ${PKG_NAME} не найден в репозитории"
        return 1
    fi
}

echo "=== Установка пакетов ==="
echo ""

install_pkg "zstd"
echo ""

install_pkg "tor"
echo ""

install_pkg "obfs4proxy-tor"
echo ""

install_pkg "webtunnel-tor"
echo ""

echo "=== Результат ==="
echo ""
echo "Установленные пакеты:"
pkg info | grep -E "^tor-|obfs4|webtunnel|zstd" || echo "  (не найдены)"

echo ""
echo "Бинарные файлы:"
for bin in tor obfs4proxy webtunnel-tor-client; do
    if [ -x "/usr/local/bin/$bin" ]; then
        echo "  ✓ /usr/local/bin/$bin"
    else
        echo "  ✗ $bin не найден"
    fi
done

echo ""
echo "=== Готово ==="
ENDSCRIPT

chmod +x /root/install_tor_plugins.sh
```

Запуск:

```bash
/root/install_tor_plugins.sh
```

### Ручная установка с указанием конкретных версий

Если автоматический поиск не работает, установите пакеты вручную:

```bash
# Для FreeBSD 14 amd64 (замените версии на актуальные):
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/zstd-1.5.6.pkg
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/tor-0.4.8.12.pkg
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/obfs4proxy-tor-0.0.14_17.pkg
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/webtunnel-tor-0.0.1_10.pkg

# Для FreeBSD 13 amd64:
# Замените FreeBSD:14:amd64 на FreeBSD:13:amd64
```

> 💡 **Совет:** Чтобы узнать актуальные версии, откройте в браузере:
> https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/
> и найдите нужные пакеты.

---

## 🌉 Настройка мостов

### Когда нужны мосты?

Мосты необходимы, если:

- ❌ Прямые подключения к Tor заблокированы
- ❌ Провайдер использует DPI для блокировки Tor
- ❌ Вы находитесь в стране с жёсткой цензурой

### Типы мостов

| Тип | Описание | Когда использовать |
|-----|----------|-------------------|
| **obfs4** | Маскирует трафик под случайные данные | Базовая блокировка Tor |
| **webtunnel** | Маскирует трафик под HTTPS | DPI блокирует obfs4 |
| **snowflake** | Использует WebRTC | Заблокированы другие мосты |

### Получение мостов

#### Способ 1: Веб-сайт

1. Перейдите на https://bridges.torproject.org/
2. Выберите тип транспорта (obfs4 или webtunnel)
3. Решите CAPTCHA
4. Скопируйте bridge-строки

#### Способ 2: Email

1. Отправьте email на `bridges@torproject.org`
2. Тема: `get transport obfs4` или `get transport webtunnel`
3. Используйте Gmail, Yahoo или Riseup

#### Способ 3: Telegram

1. Бот [@GetBridgesBot](https://t.me/GetBridgesBot)
2. Команда `/bridges`
3. Выберите тип транспорта

### Формат bridge-строк

```ini
# obfs4
Bridge obfs4 IP:PORT FINGERPRINT cert=CERTIFICATE iat-mode=0

# webtunnel
Bridge webtunnel IP:PORT FINGERPRINT url=https://domain.com/path
```

---

## 📝 Конфигурация torrc

### Расположение

```
/usr/local/etc/tor/torrc
```

### Полный пример конфигурации

```ini
# ═══════════════════════════════════════════════════════════════════════════
#                    AntiZapret TOR Configuration
# ═══════════════════════════════════════════════════════════════════════════

# Логирование
Log notice file /var/log/tor/notices.log

# SOCKS5 прокси
SocksPort 127.0.0.1:9050
SocksPort 192.168.1.1:9050                  # Замените на ваш LAN IP

# DNS через Tor
DNSPort 127.0.0.1:9053
DNSPort 192.168.1.1:9053                    # Замените на ваш LAN IP

# Прозрачный прокси
TransPort 9040

# Виртуальные адреса
VirtualAddrNetworkIPv4 10.192.0.0/10
AutomapHostsOnResolve 1

# Режим демона
RunAsDaemon 1

# Только клиент (не relay)
ExitPolicy reject *:*
ExitPolicy reject6 *:*
ExitRelay 0

# Исключаем узлы СНГ
ExcludeNodes {RU},{BY},{KG},{KZ},{UZ},{TJ},{TM},{TR},{AZ},{AM},{GE},{MD},{UA}
ExcludeExitNodes {RU},{BY},{KG},{KZ},{UZ},{TJ},{TM},{TR},{AZ},{AM},{GE},{MD},{UA}
StrictNodes 1

# IPv6
ClientUseIPv6 1
ClientUseIPv4 1

# Транспортные плагины
ClientTransportPlugin obfs4 exec /usr/local/bin/obfs4proxy managed
ClientTransportPlugin webtunnel exec /usr/local/bin/webtunnel-tor-client

# Мосты (раскомментируйте при необходимости)
UseBridges 0
# Bridge obfs4 IP:PORT FINGERPRINT cert=CERT iat-mode=0
# Bridge webtunnel IP:PORT FINGERPRINT url=https://example.com/

# Производительность
HeartbeatPeriod 1 hours
CircuitBuildTimeout 30
NumEntryGuards 3
MaxMemInQueues 256 MB
```

### Проверка

```bash
tor --verify-config
# Ожидаемый вывод: Configuration was valid
```

---

## 🔌 Транспортные плагины

### obfs4

Маскирует Tor-трафик под случайные данные.

```
Клиент → obfs4proxy → [случайные байты] → obfs4 bridge → Tor
```

**Проверка:**
```bash
which obfs4proxy
obfs4proxy -version 2>&1 | head -1
```

### webtunnel

Маскирует Tor-трафик под обычный HTTPS.

```
Клиент → webtunnel → [HTTPS трафик] → webtunnel bridge → Tor
```

**Проверка:**
```bash
which webtunnel-tor-client
```

### Сравнение

| Характеристика | obfs4 | webtunnel |
|----------------|-------|-----------|
| Скорость | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Скрытность | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Доступность мостов | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 📊 Мониторинг

### Логи

```bash
# Текущие логи
tail -f /var/log/tor/notices.log

# Статус загрузки
grep "Bootstrapped" /var/log/tor/notices.log

# Ошибки
grep -i "error\|warn" /var/log/tor/notices.log
```

### Статус

```bash
# Сервис
service tor status

# Порты
sockstat -4l | grep tor

# Проверка через SOCKS
curl --socks5 127.0.0.1:9050 https://check.torproject.org/api/ip
```

### Bootstrap стадии

| Процент | Описание |
|---------|----------|
| 0-10% | Подключение к каталогу |
| 10-80% | Загрузка дескрипторов |
| 80-100% | Построение цепочек |
| **100%** | **Готово** |

---

## 🔧 Решение проблем

### Tor не запускается

```bash
# Проверка конфигурации
tor --verify-config

# Права доступа
chown -R _tor:_tor /var/log/tor /var/run/tor /var/db/tor
chmod 700 /var/db/tor

# Порты заняты?
sockstat -4l | grep -E "9040|9050|9053"
```

### Плагин не найден

```
[warn] Could not launch managed proxy executable
```

**Решение:** Установите пакет из репозитория FreeBSD (см. [Установка](#-установка)).

### Застрял на Bootstrap

| Симптом | Причина | Решение |
|---------|---------|---------|
| 5-10% | Tor заблокирован | Настройте мосты |
| 15% | Firewall | Проверьте правила |
| 50% | Медленная сеть | Увеличьте таймауты |

### Сайты не открываются

```bash
service tor status                                    # Tor запущен?
grep "Bootstrapped 100%" /var/log/tor/notices.log    # Подключён?
sockstat -4l | grep tor                              # Порты?
pfctl -t AntiZapret_IPs -T show | head               # Alias?
```

---

## 🛡️ Безопасность

### Рекомендации

| ✅ Делайте | ❌ Не делайте |
|-----------|---------------|
| Обновляйте Tor | Не используйте как exit-ноду |
| Используйте мосты | Не открывайте ControlPort |
| Исключайте узлы СНГ | Не отключайте StrictNodes |

### Что видит провайдер

| Режим | Видит |
|-------|-------|
| Без мостов | Подключения к Tor-нодам |
| obfs4 | Зашифрованный трафик |
| webtunnel | HTTPS к обычному сайту |

---

## 📚 Ресурсы

| Ресурс | URL |
|--------|-----|
| Tor Project | https://www.torproject.org/ |
| Мосты | https://bridges.torproject.org/ |
| FreeBSD Packages | https://pkg.freebsd.org/ |
| obfs4proxy порт | https://cgit.freebsd.org/ports/tree/security/obfs4proxy-tor |
| webtunnel порт | https://cgit.freebsd.org/ports/tree/security/webtunnel-tor |

---

## 💖 Поддержать проект

<div align="center">

**USDT (TRC20)**

```
TCyZuUjX3ymFmrDPxTmeSNPMuuWRDtviFy
```

Основано на [Limych/antizapret](https://github.com/Limych/antizapret)

**[⬆ Вернуться наверх](#-настройка-tor-для-antizapret)**

</div>
