# ❓ Часто задаваемые вопросы (FAQ)

Ответы на популярные вопросы об AntiZapret для OPNsense/FreeBSD.

---

## 📋 Содержание

- [Общие вопросы](#-общие-вопросы)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Работа системы](#-работа-системы)
- [Проблемы и решения](#-проблемы-и-решения)
- [Безопасность](#-безопасность)
- [Производительность](#-производительность)

---

## 🌐 Общие вопросы

### Что такое AntiZapret?

**AntiZapret** — это прозрачный TOR шлюз для локальной сети. Он автоматически направляет трафик через Tor, не требуя настройки на клиентских устройствах.

### Как это работает?

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   1. Клиент запрашивает сайт                                     │
│   2. OPNsense проверяет IP сайта по списку AntiZapret            │
│   3. Если IP в списке → трафик идёт через Tor                    │
│   4. Если IP не в списке → прямое подключение                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### Нужно ли что-то настраивать на клиентах?

**Нет!** Это главное преимущество системы. Все устройства в сети (компьютеры, телефоны, Smart TV, игровые консоли) автоматически получают доступ к заблокированным ресурсам без каких-либо настроек.

### Поддерживается ли HTTPS?

**Да**, полная поддержка HTTPS. Система работает на уровне IP-адресов, поэтому шифрование не нарушается и сертификаты на клиентах устанавливать не нужно.

### Какие устройства поддерживаются?

Любые устройства в локальной сети:

| Устройство | Поддержка |
|------------|-----------|
| Windows/Mac/Linux | ✅ |
| iPhone/Android | ✅ |
| Smart TV | ✅ |
| PlayStation/Xbox | ✅ |
| IoT устройства | ✅ |

### Бесплатно ли это?

**Да**, полностью бесплатно. Используется сеть Tor, которая поддерживается волонтёрами по всему миру.

---

## 📦 Установка

### Какие системные требования?

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| **ОС** | FreeBSD 13.x | FreeBSD 14.x |
| **Платформа** | OPNsense 23.x | OPNsense 24.x |
| **RAM** | 512 MB свободно | 1 GB свободно |
| **Диск** | 100 MB | 500 MB |

### Работает ли на pfSense?

Официально не тестировалось, но должно работать, так как pfSense также основан на FreeBSD. Могут потребоваться небольшие адаптации путей и команд.

### Работает ли на OpenWrt/MikroTik?

Нет, данный проект предназначен только для OPNsense/FreeBSD. Для других платформ существуют альтернативные решения.

### Почему obfs4proxy и webtunnel нет в репозитории OPNsense?

Эти пакеты отсутствуют в стандартном репозитории OPNsense, но доступны в репозитории FreeBSD. Установщик автоматически скачивает их оттуда:

```bash
# Пример установки из репозитория FreeBSD
pkg add https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/obfs4proxy-tor-0.0.14_17.pkg
```

### Как найти актуальные версии пакетов?

Откройте в браузере репозиторий FreeBSD:

```
https://pkg.freebsd.org/FreeBSD:14:amd64/latest/All/
```

Найдите нужные пакеты: `tor`, `obfs4proxy-tor`, `webtunnel-tor`.

Или через командную строку:

```bash
OS_VERSION=$(uname -r | cut -d. -f1)
ARCH=$(uname -m)
PKG_BASE="https://pkg.freebsd.org/FreeBSD:${OS_VERSION}:${ARCH}/latest/All"

fetch -qo - "${PKG_BASE}/" | grep -oE '(tor|obfs4proxy-tor|webtunnel-tor)-[0-9][^"]*\.pkg' | sort -u
```

### Скрипт не запускается (ошибка `: not found`)

Это происходит из-за Windows-окончаний строк (CRLF вместо LF) при копировании из браузера.

**Решение:**

```bash
# Вариант 1: через sed
sed -i '' 's/\r$//' имя_скрипта.sh

# Вариант 2: через tr
tr -d '\r' < имя_скрипта.sh > temp.sh && mv temp.sh имя_скрипта.sh

# Вариант 3: создайте скрипт через cat << 'EOF'
cat > script.sh << 'EOF'
#!/bin/sh
echo "Hello"
EOF
```

---

## ⚙️ Настройка

### Как изменить LAN IP после установки?

Отредактируйте файл `/usr/local/etc/tor/torrc`:

```bash
nano /usr/local/etc/tor/torrc
```

Замените старый IP на новый в строках:

```ini
SocksPort 192.168.1.1:9050      # Ваш новый IP
DNSPort 192.168.1.1:9053        # Ваш новый IP
```

Перезапустите Tor:

```bash
service tor restart
```

Также обновите URL в OPNsense Alias.

### Как добавить свои IP/домены в список?

#### Добавление IP вручную:

```bash
# Добавить IP
echo "1.2.3.4" >> /usr/local/www/ipfw_antizapret.dat

# Добавить подсеть
echo "1.2.3.0/24" >> /usr/local/www/ipfw_antizapret.dat

# Обновить alias в памяти
pfctl -t AntiZapret_IPs -T add 1.2.3.4
```

#### Добавление домена:

```bash
# Узнать IP домена
host example.com

# Добавить полученные IP
echo "93.184.216.34" >> /usr/local/www/ipfw_antizapret.dat
```

### Как исключить сайт из списка?

```bash
# Удалить IP из файла
sed -i '' '/1\.2\.3\.4/d' /usr/local/www/ipfw_antizapret.dat

# Удалить из alias в памяти
pfctl -t AntiZapret_IPs -T delete 1.2.3.4
```

### Как изменить страну выхода Tor?

Отредактируйте `/usr/local/etc/tor/torrc`:

```ini
# Использовать только определённые страны для exit-нод
ExitNodes {NL},{DE},{SE},{CH}

# Или исключить страны
ExcludeExitNodes {RU},{BY},{KZ}
```

Коды стран:

| Код | Страна |
|-----|--------|
| {US} | США |
| {DE} | Германия |
| {NL} | Нидерланды |
| {SE} | Швеция |
| {CH} | Швейцария |
| {PL} | Польша |

### Как включить мосты?

1. Получите мосты на https://bridges.torproject.org/
2. Отредактируйте `/usr/local/etc/tor/torrc`:

```ini
UseBridges 1
Bridge obfs4 IP:PORT FINGERPRINT cert=CERT iat-mode=0
```

3. Перезапустите Tor:

```bash
service tor restart
```

### Как часто обновляется список IP?

По умолчанию — раз в сутки через cron (в 4:00). Можно изменить в:

```
OPNsense → System → Settings → Cron
```

Для ручного обновления:

```bash
/root/antizapret/antizapret.pl > /usr/local/www/ipfw_antizapret.dat
```

---

## 🔄 Работа системы

### Как проверить, что Tor работает?

```bash
# Статус сервиса
service tor status

# Проверка подключения к сети Tor
grep "Bootstrapped 100%" /var/log/tor/notices.log

# Проверка через SOCKS-прокси
curl --socks5 127.0.0.1:9050 https://check.torproject.org/api/ip
```

### Как проверить, что сайт идёт через Tor?

1. Откройте https://check.torproject.org/ с клиентского устройства
2. Если видите "Congratulations. This browser is configured to use Tor" — работает

Или через командную строку:

```bash
# Узнать IP сайта
host blocked-site.com

# Проверить, есть ли IP в списке
grep "ПОЛУЧЕННЫЙ_IP" /usr/local/www/ipfw_antizapret.dat
```

### Сколько IP-адресов в списке?

```bash
wc -l /usr/local/www/ipfw_antizapret.dat
```

Обычно около 15,000–20,000 IP-адресов.

### Какие порты использует система?

| Порт | Назначение |
|------|------------|
| 9040 | Transparent Proxy (для перенаправленного трафика) |
| 9050 | SOCKS5 Proxy |
| 9053 | DNS через Tor |

### Можно ли использовать SOCKS-прокси напрямую?

**Да**, для приложений, поддерживающих SOCKS5:

```
Адрес: 192.168.1.1 (ваш LAN IP)
Порт: 9050
Тип: SOCKS5
```

---

## 🔧 Проблемы и решения

### Tor не запускается

```bash
# 1. Проверьте конфигурацию
tor --verify-config

# 2. Проверьте права доступа
chown -R _tor:_tor /var/log/tor /var/run/tor /var/db/tor
chmod 700 /var/db/tor

# 3. Проверьте, не заняты ли порты
sockstat -4l | grep -E "9040|9050|9053"

# 4. Просмотрите логи
tail -50 /var/log/tor/notices.log
```

### Tor застрял на Bootstrap

| Процент | Возможная причина | Решение |
|---------|-------------------|---------|
| 5-10% | Tor заблокирован провайдером | Настройте мосты (obfs4/webtunnel) |
| 15% | Firewall блокирует соединения | Проверьте правила OPNsense |
| 50% | Медленная сеть | Увеличьте таймауты в torrc |

### Сайты не открываются

**Чеклист:**

```bash
# 1. Tor работает?
service tor status

# 2. Tor подключился к сети?
grep "Bootstrapped 100%" /var/log/tor/notices.log

# 3. Порты слушают?
sockstat -4l | grep tor

# 4. Alias загружен?
pfctl -t AntiZapret_IPs -T show | wc -l

# 5. IP сайта в списке?
host blocked-site.com
grep "IP_АДРЕС" /usr/local/www/ipfw_antizapret.dat

# 6. NAT-правило работает?
# Проверьте в OPNsense: Firewall → NAT → Port Forward
```

### Ошибка "Could not launch managed proxy executable"

Транспортный плагин (obfs4proxy или webtunnel) не установлен.

**Решение:**

```bash
# Проверьте наличие
which obfs4proxy
which webtunnel-tor-client

# Если не найден — установите из репозитория FreeBSD
OS_VERSION=$(uname -r | cut -d. -f1)
ARCH=$(uname -m)
PKG_BASE="https://pkg.freebsd.org/FreeBSD:${OS_VERSION}:${ARCH}/latest/All"

# Найти актуальную версию
fetch -qo - "${PKG_BASE}/" | grep -oE 'obfs4proxy-tor-[0-9][^"]*\.pkg' | tail -1

# Установить (замените версию на актуальную)
pkg add ${PKG_BASE}/obfs4proxy-tor-0.0.14_17.pkg
```

### Alias не обновляется

```bash
# Ручное обновление
/root/antizapret/antizapret.pl > /usr/local/www/ipfw_antizapret.dat

# Проверка файла
head -10 /usr/local/www/ipfw_antizapret.dat
wc -l /usr/local/www/ipfw_antizapret.dat

# Обновление alias в памяти
pfctl -t AntiZapret_IPs -T replace -f /usr/local/www/ipfw_antizapret.dat
```

### Медленная скорость через Tor

Tor работает медленнее прямого соединения — это нормально. Для улучшения:

1. **Используйте быстрые exit-ноды:**
   ```ini
   ExitNodes {NL},{DE},{SE}
   ```

2. **Увеличьте количество guard-нод:**
   ```ini
   NumEntryGuards 5
   ```

3. **Оптимизируйте таймауты:**
   ```ini
   CircuitBuildTimeout 15
   MaxCircuitDirtiness 300
   ```

### После перезагрузки Tor не запускается

Проверьте автозапуск:

```bash
# Проверка
grep tor_enable /etc/rc.conf

# Включение
sysrc tor_enable="YES"

# Запуск
service tor start
```

---

## 🛡️ Безопасность

### Безопасно ли использовать Tor?

**Да**, при правильной настройке:

- Мы используем Tor только как клиент (не exit-нода)
- Исключены узлы из СНГ и соседних стран
- Трафик зашифрован на всём пути

### Что видит мой провайдер?

| Режим | Что видит провайдер |
|-------|---------------------|
| Без мостов | Подключения к известным Tor-нодам |
| С obfs4 | Зашифрованный трафик на случайные IP |
| С webtunnel | Обычный HTTPS-трафик |

### Что видит целевой сайт?

| Информация | Видит? |
|------------|--------|
| Ваш реальный IP | ❌ Нет |
| IP exit-ноды Tor | ✅ Да |
| Что вы используете Tor | ✅ Да (можно определить) |
| Содержимое HTTPS | ❌ Нет |

### Могут ли заблокировать за использование Tor?

В большинстве стран использование Tor легально. Однако в некоторых странах (Китай, Иран и др.) Tor заблокирован. В таких случаях используйте мосты.

### Нужен ли VPN вместе с Tor?

Для AntiZapret VPN не обязателен. Однако VPN перед Tor может скрыть сам факт использования Tor от провайдера.

---

## ⚡ Производительность

### Насколько Tor медленнее прямого соединения?

Tor добавляет задержку 50-200 мс из-за маршрутизации через 3 узла. Скорость обычно составляет 1-10 Мбит/с, что достаточно для веб-сёрфинга и видео в SD/HD.

### Tor не подходит для:

- ❌ Торрентов (медленно и нагружает сеть)
- ❌ Онлайн-игр (высокий пинг)
- ❌ 4K видео (недостаточная скорость)

### Tor подходит для:

- ✅ Веб-сёрфинга
- ✅ Социальных сетей
- ✅ Мессенджеров
- ✅ Видео в SD/HD качестве

### Как уменьшить нагрузку на роутер?

```ini
# В torrc:
MaxMemInQueues 128 MB    # Уменьшить использование памяти
NumEntryGuards 2         # Меньше guard-нод
```

### Сколько клиентов может обслуживать система?

Зависит от мощности роутера. Типичный домашний роутер с OPNsense легко обслуживает 10-20 одновременных клиентов.

---

## 🔄 Обновление и обслуживание

### Как обновить AntiZapret?

```bash
cd /root/antizapret
git pull
chmod +x install.sh
```

### Как обновить Tor?

```bash
# Из репозитория OPNsense (если доступно)
pkg update
pkg upgrade tor

# Или из репозитория FreeBSD
OS_VERSION=$(uname -r | cut -d. -f1)
ARCH=$(uname -m)
PKG_BASE="https://pkg.freebsd.org/FreeBSD:${OS_VERSION}:${ARCH}/latest/All"

# Найти новую версию
fetch -qo - "${PKG_BASE}/" | grep -oE 'tor-[0-9][^"]*\.pkg' | tail -1

# Установить
pkg add ${PKG_BASE}/tor-НОВАЯ_ВЕРСИЯ.pkg
service tor restart
```

### Как полностью удалить AntiZapret?

```bash
# Остановить Tor
service tor stop

# Удалить автозапуск
sysrc -x tor_enable

# Удалить пакеты
pkg delete tor obfs4proxy-tor webtunnel-tor

# Удалить файлы
rm -rf /root/antizapret
rm -f /usr/local/www/ipfw_antizapret.dat
rm -f /usr/local/etc/tor/torrc
rm -rf /var/log/tor /var/run/tor /var/db/tor

# Удалить OPNsense actions
rm -f /usr/local/opnsense/service/conf/actions.d/actions_antizapret.conf
rm -f /usr/local/opnsense/service/conf/actions.d/actions_tor.conf
service configd restart
```

Также удалите в OPNsense GUI:
- Firewall → NAT → Port Forward (правило AntiZapret)
- Firewall → Aliases (AntiZapret_IPs)
- System → Settings → Cron (задание обновления)

---

## 📚 Дополнительная документация

| Документ | Описание |
|----------|----------|
| [README.md](README.md) | Общее описание проекта |
| [QUICKSTART.md](QUICKSTART.md) | Быстрый старт |
| [INSTALL.md](INSTALL.md) | Полная инструкция |
| [TOR.md](TOR.md) | Настройка Tor |
| [WireGuard.md](WireGuard.md) | Альтернатива с VPN |
| [CHANGELOG.md](CHANGELOG.md) | История изменений |

---

## 💖 Поддержать проект

Если AntiZapret оказался полезен:

<div align="center">

**USDT (TRC20)**

```
TCyZuUjX3ymFmrDPxTmeSNPMuuWRDtviFy
```

</div>

---

<div align="center">

Основано на [Limych/antizapret](https://github.com/Limych/antizapret)

**[⬆ Вернуться наверх](#-часто-задаваемые-вопросы-faq)**

</div>
